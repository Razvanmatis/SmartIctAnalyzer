<UserControl x:Class="Uniconverter.Gui.Views.ApplicationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Uniconverter.Gui"
             xmlns:views="clr-namespace:Uniconverter.Gui.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="1080">

    <UserControl.Resources>
       <!--<DataTemplate  DataType="{x:Type local:TestPointsTabelViewModel}">
            <views:TestPointsTableView />
        </DataTemplate> -->

    </UserControl.Resources>



    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>


        <ContentControl Content="{Binding CurrentSidePageViewModel}"/>


        <!-- END ToolBar -->
        <Button Grid.Column="1" Content="Next Window" Command="{Binding GoTo}"></Button>

        
    </Grid>
</UserControl>
