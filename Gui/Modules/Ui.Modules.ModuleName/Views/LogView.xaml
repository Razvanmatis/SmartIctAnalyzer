<UserControl x:Class="Ui.Modules.ModuleName.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Ui.Modules.ModuleName.Views"
             xmlns:prism="http://prismlibrary.com/"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="800"
             prism:ViewModelLocator.AutoWireViewModel="true"
             Height="300">
    <UserControl.Resources>
        <DataTemplate x:Key="DescrWrapCell">
            <TextBlock Text="{Binding Path=Message}" TextWrapping="Wrap"/>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="300" />
        </Grid.RowDefinitions>
        <ScrollViewer Grid.ColumnSpan="2" VerticalAlignment="Stretch" VerticalScrollBarVisibility="Auto">
            <ListView Name="listView" ItemsSource="{Binding Messages}" SelectedItem="{Binding SelectedMessage}" SelectionChanged="SelectionChanged">
                <ListView.ItemContainerStyle>
                    <Style TargetType="{x:Type ListViewItem}">
                        <Setter Property="Background" Value="{Binding Background}" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView AllowsColumnReorder="true" ColumnHeaderToolTip="Log messages">
                        <GridViewColumn DisplayMemberBinding="{Binding Path=DateTime}" Header="Date and time" Width="Auto"/>
                        <GridViewColumn CellTemplate="{StaticResource DescrWrapCell}" Header="Message" />
                    </GridView>
                </ListView.View>
                <ListView.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Copy" Command="{Binding CopyMessage}"/>
                    </ContextMenu>
                </ListView.ContextMenu>
            </ListView>
        </ScrollViewer>
    </Grid>
</UserControl>
