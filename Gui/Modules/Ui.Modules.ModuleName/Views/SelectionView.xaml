<Window x:Class="Ui.Modules.ModuleName.Views.SelectionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:h="clr-namespace:Ui.Modules.ModuleName.Helper"
        prism:ViewModelLocator.AutoWireViewModel="true"
        mc:Ignorable="d"
        Title="{Binding Label, UpdateSourceTrigger=PropertyChanged}" Height="170" Width="300"
        h:WindowAttached.WindowProperty="{Binding WindowCommand}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Label HorizontalAlignment="Center" Content="{Binding Label, UpdateSourceTrigger=PropertyChanged}" />
        <ComboBox HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Margin="5" Grid.Row="1" ItemsSource="{Binding Items, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedItem, UpdateSourceTrigger=PropertyChanged }" />
        <Button HorizontalAlignment="Center" Grid.Row="2" Content="Ok" Command="{Binding OkCommand, UpdateSourceTrigger=PropertyChanged}" />
    </Grid>
</Window>
