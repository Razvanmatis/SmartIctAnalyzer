<Window x:Class="Ui.Modules.ModuleName.Views.InputBoxView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ui.Modules.ModuleName.Views"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:h="clr-namespace:Ui.Modules.ModuleName.Helper"
        prism:ViewModelLocator.AutoWireViewModel="true"
        mc:Ignorable="d"
        Title="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
        Height="{Binding Height, UpdateSourceTrigger=PropertyChanged}"
        Width="{Binding Width, UpdateSourceTrigger=PropertyChanged}"
        h:WindowAttached.WindowProperty="{Binding WindowCommand}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="50" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.ColumnSpan="2" HorizontalAlignment="Center" Text="{Binding Info, UpdateSourceTrigger=PropertyChanged}" Margin="5" />
        <TextBox Grid.ColumnSpan="2" HorizontalAlignment="Stretch" Grid.Row="1" Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" Margin="5" />
        <Button Content="OK" Command="{Binding ClickCommand}" Grid.Column="1" Grid.Row="2" Height="50" Width="100" Margin="150, 5, 5, 5" />
    </Grid>
</Window>
