<UserControl x:Class="Ui.Modules.ModuleName.Views.LayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:help="clr-namespace:Ui.Modules.ModuleName.Helper"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="200"
             prism:ViewModelLocator.AutoWireViewModel="true">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*" />
            <RowDefinition Height="30"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border BorderBrush="Black" BorderThickness="2" Grid.RowSpan="2" />
        <TextBlock Text="Layers:" HorizontalAlignment="Left" Margin="10, 0, 0, 0" FontSize="20" FontWeight="Bold" />
        <ListView x:Name="ListViewLayers" Grid.Row="1" ItemsSource="{Binding Layers}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Margin="5" Text="{Binding Content}" />
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                            <Setter Property="IsSelected" Value="True" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsSelected}" Value="False">
                            <Setter Property="IsSelected" Value="False" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="SelectionChanged">
                    <i:InvokeCommandAction Command="{Binding SelectionChangedLayer}" PassEventArgsToCommand="True"
                                            CommandParameter="{Binding ElementName=ListViewLayers, Path=SelectedItems}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </ListView>
        <Border Grid.Row="2" BorderBrush="Black" BorderThickness="2" Grid.RowSpan="2" />
        <TextBlock Text="Objects:" Grid.Row="2" HorizontalAlignment="Left" Margin="10, 0, 0, 0" FontSize="20" FontWeight="Bold" />
        <ListView x:Name="ListViewObjects" Grid.Row="3" ItemsSource="{Binding Objects}">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="SelectionChanged">
                    <i:InvokeCommandAction Command="{Binding SelectionChangedObjects}" PassEventArgsToCommand="True"
                                            CommandParameter="{Binding ElementName=ListViewObjects, Path=SelectedItems}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </ListView>
    </Grid>
</UserControl>
