<UserControl x:Class="Ui.Modules.ModuleName.Views.LayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="200"
             prism:ViewModelLocator.AutoWireViewModel="true">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*" />
            <RowDefinition Height="30"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border BorderBrush="Black" BorderThickness="2" Grid.RowSpan="2" />
        <TextBlock Text="Layers:" HorizontalAlignment="Left" Margin="10, 0, 0, 0" FontSize="20" FontWeight="Bold" />
        <ListView x:Name="ListView" Grid.Row="1" ItemsSource="{Binding Layers}" SelectedItem="{Binding SelectedLayer, UpdateSourceTrigger=PropertyChanged}">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="SelectionChanged">
                    <i:InvokeCommandAction Command="{Binding SelectionChanged}" PassEventArgsToCommand="True"
                                            CommandParameter="{Binding ElementName=ListView, Path=SelectedItems}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </ListView>
        <Border Grid.Row="2" BorderBrush="Black" BorderThickness="2" Grid.RowSpan="2" />
        <TextBlock Text="Objects:" Grid.Row="2" HorizontalAlignment="Left" Margin="10, 0, 0, 0" FontSize="20" FontWeight="Bold" />
        <ListView x:Name="ListViewObjects" Grid.Row="3" ItemsSource="{Binding Objects}" SelectedItem="{Binding SelectedObject, UpdateSourceTrigger=PropertyChanged}">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="SelectionChanged">
                    <i:InvokeCommandAction Command="{Binding SelectionChangedObjects}" PassEventArgsToCommand="True"
                                            CommandParameter="{Binding ElementName=ListViewObjects, Path=SelectedItems}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </ListView>
    </Grid>
</UserControl>
